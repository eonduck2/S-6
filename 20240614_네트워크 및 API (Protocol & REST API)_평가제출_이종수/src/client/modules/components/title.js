import component from "./component.js";

/**
 * TYPE[FUNCTION]
 *
 * STATUS[O]
 *
 * TODO: 함수 정의
 *
 * NOTE: 1. title 컴포넌트
 */

export default (element) => {
  const title = component(element);
  return title;
};
